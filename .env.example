# ============================================
# Supabase 환경 변수
# ============================================

# 클라이언트 & 서버 공통 사용 (공개 가능)
# Supabase 프로젝트 URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase anon/public key (브라우저에서 사용 가능)
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-anon-public-key

# ============================================
# 서버 전용 환경 변수 (절대 NEXT_PUBLIC 접두사 금지)
# ============================================

# Supabase Service Role Key (서버에서만 사용, 고권한)
# 주의: 이 키는 절대 클라이언트에 노출되면 안 됩니다
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Webhook 검증용 비밀 키 (Supabase Database Webhook 설정과 동일하게)
SUPABASE_WEBHOOK_SECRET=your-random-webhook-secret

# ============================================
# 애플리케이션 설정
# ============================================

# 앱 URL (배포 환경에서 설정)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Node 환경
NODE_ENV=development
